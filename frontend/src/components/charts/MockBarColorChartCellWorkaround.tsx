import React from 'react';

// Recharts typing note:
// <Cell /> does not always work with strict TS typings depending on version.
// This component exists only as a placeholder if you need custom cell rendering.
export function MockBarColorChartCellWorkaround() {
  return null;
}

